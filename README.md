# For Study 2.0

A study terminal. You may use linux-like commands. For futher information, please run `help` inside the console.

## Set up Console

1. Clone it down to local
    ```bash
    git clone <url>
    cd for-study-new
    ```
2. Run it
    ```bash
    python main.py
    ```
